model Mood {
  id           String   @id @default(cuid())
  createdAt    DateTime @default(now())
  lat          Float
  lng          Float
  mood         String
  energy       Int
  city         String?
  country      String?
  message      String?
  deleteToken  String?  @unique   // <- VORÜBERGEHEND optional!
}

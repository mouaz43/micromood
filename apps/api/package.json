{
  "name": "micromood",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "build": "npm run -w apps/api build && npm run -w apps/web build",
    "dev": "concurrently -k -n API,WEB -c yellow,cyan \"npm run -w apps/api dev\" \"npm run -w apps/web dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
